# Leave blank for same-origin API/SSE. Set absolute URLs when the Vite frontend
# is served separately from the backend.
VITE_API_BASE_URL=
# Accepts a path such as /events or an absolute URL such as https://api.example/events.
VITE_SOCKET_URL=
