# 36chan Frontend

Vite project that hosts the migrated 36chan browser UI.

## Run

```bash
npm run dev
```

The dev server proxies `/api` and `/events` to the backend on `http://localhost:3000`.

## Build

```bash
npm run build
```
